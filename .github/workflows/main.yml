name: workflow for my test file
on: push
jobs:
  build:
    name: Test action
    runs-on: ubuntu-latest
    steps:
      - uses: action/checkout@v1
      - uses: ./action-a
        with:
          MY_NAME: "Ilkka"
